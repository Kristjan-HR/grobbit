package test;

import "fmt";

func foo() int {
	return 1;
};

func main() {
	var x int;
	var y int;
	var ok bool;

	x = 10 + 2 * 3;
	y = (10 + 2) * 3;
	ok = true;

	if -x < y && !false || x == 10 {
		foo();
		fmt.Println(x, y);
	};
};